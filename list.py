fruits=["mangoes","apples","oranges","avocado","bananas"]
#def Automate():
       #while True:
        #item=str(input("Enter a fruit.\n"))
        #fruits.append(item)
        #print(fruits)
#Automate()

#fruits.sort()
#print(fruits)

# The code `word = "hello"` initializes a variable `word` with the string value "hello".
word= "hello"
# The code `for x in word: print(x)` is iterating over each character in the string variable `word`
# and printing each character on a new line. In this case, it will print each letter of the word
# "hello" on a separate line.
#this prints each character in the string which is stores
for x in word:
    print(x)